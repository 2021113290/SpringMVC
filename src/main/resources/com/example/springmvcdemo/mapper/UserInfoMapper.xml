<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springmvcdemo.mapper.UserInfoMapper">
    <select id="getUserInfoById" resultType="com.example.springmvcdemo.entity.UserInfo">
        select * from userinfo where id = #{id}
    </select>
    <select id="getAll" resultType="com.example.springmvcdemo.entity.UserInfo">
        select * from userinfo
    </select>
    <insert id="add">
        insert into userinfo(username,password,createtime,updatetime) values (#{username},#{password},#{createtime},#{updatetime})
    </insert>
    <update id="update">
        update userinfo set username=#{name} where id=#{id}
    </update>
    <delete id="deleteById">
        delete from userinfo where id=#{id}
    </delete>
    <insert id="add2">
        insert into userinfo(username,
        <if test="photo!=null">
            photo,
        </if>
        password)
        values (#{username},
        <if test="photo!=null">
            #{photo},
        </if>
        #{password})
    </insert>
    <insert id="add3">
        insert into userinfo
<trim prefix="(" suffix=")" suffixOverrides=",">
    <if test="username!=null">
        username,
    </if>
            <if test="password!=null">
                password,
            </if>
            <if test="photo!=null">
                photo,
            </if>
</trim>
            values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username!=null">
                #{username},
            </if>
            <if test="password!=null">
                #{password},
            </if>
            <if test="photo!=null">
                #{photo},
            </if>
        </trim>
    </insert>
    <select id="getListByCondition" resultType="com.example.springmvcdemo.entity.UserInfo">
        select * from userinfo
        <trim prefix="where" prefixOverrides="and">
                        <if test="username!=null">
                            username=#{username}
                        </if>
                        <if test="password!=null">
                             and
                            password=#{password}
                        </if>
        </trim>
<!--        <where>-->
<!--            <if test="username!=null">-->
<!--                username=#{username}-->
<!--            </if>-->
<!--            <if test="password!=null">-->
<!--                and-->
<!--                password=#{password}-->
<!--            </if>-->
<!--        </where>-->
    </select>
    <update id="update2">
        update userinfo
        <set>
            <if test="username!=null">
                username=#{username},
            </if>
            <if test="password!=null">
                password=#{password},
            </if>
            <if test="photo!=null">
                photo=#{photo},
            </if>
        </set>
            where id=#{id}
    </update>
    <delete id="delete2">
        delete from userinfo where id in
        <foreach collection="ids" open="(" close=")" item="id" separator=",">
            #{id}
        </foreach>
    </delete>
</mapper>
